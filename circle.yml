dependencies:
  pre:
    - sudo apt-add-repository -y ppa:yandex-qatools/allure-framework
    - sudo apt-get update
    - sudo apt-get install allure-cli

test:
    post:
    - allure generate $CIRCLE_TEST_REPORTS/allure -o $CIRCLE_TEST_REPORTS/allure-test -v 1.4.3

